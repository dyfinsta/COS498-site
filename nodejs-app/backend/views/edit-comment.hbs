<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Comment</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  {{> nav}}
  <main>
    <h2>Edit Comment</h2>
    
    {{#if error}}
    <div class="error">
      {{error}}
    </div>
    {{/if}}
    
    <form method="POST" action="/comment/{{comment.id}}/edit">
      <div>
        <label for="content">Comment:</label>
        <textarea id="content" name="content" rows="5" required>{{comment.content}}</textarea>
      </div>
      <div class="form-buttons">
        <button type="submit">Update Comment</button>
        <a href="/comments" class="button-secondary">Cancel</a>
      </div>
    </form>
    
    <div class="warning">
      <h3>Warning</h3>
      <p>Deleting a comment is permanent and cannot be undone.</p>
      <form method="POST" action="/comment/{{comment.id}}/delete" 
            onsubmit="return confirm('Are you sure you want to delete this comment? This action cannot be undone.')">
        <button type="submit" class="button-danger">Delete Comment</button>
      </form>
    </div>
    
    <p><a href="/comments">← Back to Comments</a></p>
  </main>
  {{> footer}}
</body>
</html>